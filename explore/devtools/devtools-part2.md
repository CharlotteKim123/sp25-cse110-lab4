1. What was the bug?

   The bug was in calculateSum() because the value property of an element is a string, so the sum will be a string. 
   
2. How would you fix it?

   I would use parseInt() for num1 and num2 so the answer will be an int.
